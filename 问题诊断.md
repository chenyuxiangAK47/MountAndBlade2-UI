# QuickStartMod 问题诊断

## 🔍 问题分析

根据日志 `rgl_log_35564.txt` 第298行：

```
[13:11:22.383] Couldn't find .dll: ..\..\Modules\QuickStartMod\bin\Win64_Shipping_Client\QuickStartMod.dll
```

**问题根源**：游戏找不到 `QuickStartMod.dll` 文件。

## ✅ 解决方案

### 步骤1：编译项目

你需要创建一个 C# 项目并编译生成 DLL：

1. **创建 Visual Studio 项目**
   - 项目类型：`.NET Framework` 类库（不是 .NET Core）
   - 目标框架：`.NET Framework 4.7.2` 或更高
   - 项目名称：`QuickStartMod`

2. **添加文件**
   - 将所有 `SubModule/*.cs` 文件添加到项目
   - 确保命名空间是 `QuickStartMod`

3. **添加依赖（NuGet 或 DLL 引用）**
   - `Bannerlord.Harmony` - 从 `Modules/Bannerlord.Harmony/bin/Win64_Shipping_Client/` 引用
   - `Bannerlord.UIExtenderEx` - 从 `Modules/Bannerlord.UIExtenderEx/bin/Win64_Shipping_Client/` 引用
   - `TaleWorlds.*` - 从游戏安装目录引用

4. **编译设置**
   - 配置：`Release`
   - 平台：`Any CPU` 或 `x64`
   - 输出路径：`bin\Win64_Shipping_Client\`

5. **编译生成**
   - 编译后应该生成 `QuickStartMod.dll`
   - 确保 DLL 在 `QuickStartMod/bin/Win64_Shipping_Client/QuickStartMod.dll`

### 步骤2：验证文件结构

编译后，目录结构应该是：

```
QuickStartMod/
├── SubModule.xml
├── SubModule/
│   ├── QuickStartSubModule.cs
│   ├── QuickStartHelper.cs
│   ├── QuickStartUIExtension.cs
│   ├── QuickStartViewModelMixin.cs
│   └── QuickStartPatches.cs
└── bin/
    └── Win64_Shipping_Client/
        └── QuickStartMod.dll  ← 这个文件必须存在！
```

### 步骤3：测试

1. 启动游戏
2. 查看日志，应该看到：
   - `Loading assembly: ..\..\Modules\QuickStartMod\bin\Win64_Shipping_Client\QuickStartMod.dll`
   - `[QuickStartMod] UIExtenderEx 已启用`
   - `[QuickStartMod] PrefabExtension 已创建`
   - `[QuickStartMod] ViewModelMixin 已创建并注入到 InitialMenuVM`

## 📝 快速检查清单

- [ ] 项目已创建并命名为 `QuickStartMod`
- [ ] 所有 `.cs` 文件已添加到项目
- [ ] 依赖已正确引用（Harmony、UIExtenderEx）
- [ ] 项目已编译为 Release 配置
- [ ] `QuickStartMod.dll` 存在于 `bin/Win64_Shipping_Client/` 目录
- [ ] 在游戏启动器中启用了 `QuickStartMod`

## 🐛 如果仍然没有按钮

如果 DLL 已存在但仍然没有按钮，检查：

1. **查看日志中的 `[QuickStartMod]` 标记**
   - 如果没有这些日志，说明 DLL 没有加载或 SubModule 没有初始化

2. **检查 UIExtenderEx 是否加载**
   - 查找日志中的 `UIExtenderEx` 相关消息

3. **检查 ViewModelMixin 是否注入**
   - 查找 `[QuickStartMod] ViewModelMixin 已创建并注入到 InitialMenuVM`

4. **检查按钮 XML 是否正确**
   - PrefabExtension 的 XPath 是否正确
   - 按钮的 `Command.Click` 绑定是否正确

## 💡 提示

如果你还没有编译项目，可以：
1. 使用 Visual Studio 创建项目
2. 或者使用命令行编译（需要配置好所有依赖）
3. 或者暂时使用 MyMod 的 DLL（如果它包含了 QuickStart 代码）

