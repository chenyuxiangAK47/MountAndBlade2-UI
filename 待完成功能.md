# QuickStart Mod 待完成功能清单

## ✅ 已实现的功能

1. **主菜单"快速开始"按钮**
   - ✅ 使用 Harmony Patch 注入到 `InitialMenuVM.MenuOptions`
   - ✅ 显示在菜单顶部
   - ✅ 按钮可点击

2. **启动新游戏**
   - ✅ 点击后调用 `Game.StartNewGame(false)` 启动沙盒模式
   - ✅ 设置 `QuickStartHelper.IsQuickStartMode = true` 标志

3. **自动给予初始资源**
   - ✅ 通过 `CampaignEvents.OnSessionLaunchedEvent` 监听
   - ✅ 给予 100,000 金币
   - ✅ 显示提示消息

## ❓ 可能需要的功能（待确认）

### 1. 跳过角色创建流程 ⭐ 可能最重要

**当前状态**：点击"快速开始"后，仍然需要手动完成角色创建（选择文化、难度、出生背景等）

**可能的需求**：
- 完全跳过角色创建界面
- 使用默认角色配置直接进入游戏
- 或者自动完成角色创建（使用预设配置）

**实现方式**：
- 使用 Harmony Patch `CharacterCreationState` 相关方法
- 检测 `IsQuickStartMode` 标志
- 如果为 true，直接跳过或使用默认配置

### 2. 自定义初始资源

**当前状态**：固定给予 100,000 金币

**可能的扩展**：
- 可配置的金币数量
- 给予初始装备
- 给予初始部队
- 设置初始关系/声望

### 3. 快速开始配置选项

**可能的扩展**：
- 选择默认文化
- 选择默认难度
- 选择默认出生背景
- 保存/加载快速开始配置

### 4. 跳过教程/介绍

**可能的扩展**：
- 跳过开场动画
- 跳过教程提示
- 直接进入游戏世界

## 🎯 建议优先实现的功能

根据"快速开始"的核心目标，建议优先实现：

1. **跳过角色创建流程**（如果这是你的需求）
   - 这是"快速开始"的核心功能
   - 可以大大减少开始游戏的时间

2. **可配置的初始资源**
   - 让用户可以自定义快速开始的资源
   - 提高灵活性

## 📝 需要确认的问题

请告诉我：

1. **是否需要跳过角色创建？**
   - 如果需要，希望使用什么默认配置？（文化、难度、出生背景等）

2. **除了金币，还需要什么初始资源？**
   - 装备？
   - 部队？
   - 关系/声望？

3. **是否需要配置界面？**
   - 在主菜单添加配置按钮？
   - 或者使用配置文件？

4. **其他需求？**
   - 还有什么功能想要实现？

---

**当前代码位置**：
- 按钮点击逻辑：`SubModule/QuickStartPatches.cs` (第 115-137 行)
- 给金币逻辑：`SubModule/QuickStartSubModule.cs` (第 417-449 行)
- 配置常量：`SubModule/QuickStartHelper.cs`







